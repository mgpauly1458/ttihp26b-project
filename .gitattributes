# Git LFS is used ONLY for the simulation data under analog/out
# (analog/out/.gitattributes). Everything the Tiny Tapeout build reads --
# analog/macro/*.gds|lef, analog/lib/*.lib, docs/*.png -- is a plain git
# object: TT's checkout has no LFS, and a pointer file in place of the macro
# GDS broke Magic (CI run 34158145310).
