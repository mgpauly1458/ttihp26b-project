# Stretch goals

Not integrated, not laid out, not in `info.yaml`. Ideas + skeleton netlists only.

| doc | what |
|---|---|
| [banba_adc.md](banba_adc.md) | Banba bandgap + 1-bit sigma-delta readout, no analog pins needed |

Design files: `analog_stretch/` (mirrors `analog/` layout but nothing is hardened or verified).
